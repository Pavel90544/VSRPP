version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./products.db:/root/products.db
    environment:
      - PORT=8080
      - ENV=production
